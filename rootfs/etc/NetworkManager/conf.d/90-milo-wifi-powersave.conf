# Disable WiFi power saving (2 = disable). Powersave causes latency spikes
# that break multiroom sync and the ROC receiver. Managed by NetworkManager
# so it survives reconnects, unlike a one-shot `iw` call at boot.
[connection]
wifi.powersave = 2
