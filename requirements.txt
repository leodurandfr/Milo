fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
websockets>=14.0  # no direct import — runtime dep of uvicorn[standard] WebSocket support
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0  # required by fastapi.testclient.TestClient (tests only)
ruff>=0.6.0
aiohttp>=3.11.0
async-upnp-client>=0.47.0  # DLNA/UPnP control-point bridge (sources/dlna) — GENA eventing to the local gmediarender
dbus-next>=0.2.3
aiofiles>=23.2.1
lgpio>=0.2.2.0; platform_machine == "aarch64"  # Pi GPIO (rotary encoder) — needs liblgpio, unavailable on x86 CI
evdev>=1.7.0
Pillow>=10.0.0
python-multipart>=0.0.6
camilladsp @ git+https://github.com/HEnquist/pycamilladsp.git@v4.0.0
pyyaml>=6.0.0
shazamio>=0.7.0
aiohttp-retry>=2.9.0  # direct import in sources/radio/shazam.py (otherwise only transitive via shazamio)
audioop-lts>=0.2.2; python_version >= "3.13"  # stdlib audioop removed in 3.13 (needed by pydub via shazamio)
discid>=1.2.0
musicbrainzngs>=0.7.1
pyudev>=0.24.0  # USB hotplug detection for the Music Library storage layer (sources/music_library/storage.py)