# Milo Client - Python Dependencies
# Version: 1.3 - Hybrid approach (GitHub .deb + APT dependency resolution)

# Web framework
fastapi>=0.115.0

# ASGI server
uvicorn[standard]>=0.32.0

# HTTP client for async requests (GitHub API + downloads)
aiohttp>=3.11.0

# Async file operations (for downloading .deb files)
aiofiles>=24.0.0

# Type checking (optional but recommended)
pydantic>=2.10.0

# Multipart form parsing (required for file uploads)
python-multipart>=0.0.9

# WebSocket client for the local CamillaDSP daemon (services/camilladsp_client.py).
# Also a runtime dep of uvicorn[standard], which is why the field already has it:
# a satellite update ships the milo-client/ tree alone and never runs pip.
websockets>=14.0
