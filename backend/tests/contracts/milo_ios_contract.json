{
  "_about": "Wire contract consumed by Milo-iOS (github.com/leodurandfr/Milo-iOS), a THIRD API client beyond frontend/ and Milo-Mac. Its consumers are short-lived processes — a WidgetKit timeline entry, App Intents — so there is NO WebSocket surface at all: everything is REST, and the app can never be told about a change, only ask. If a route listed here is dropped, Milo-iOS breaks silently at runtime. THIS FILE IS THE SOURCE OF TRUTH for that contract. test_milo_ios_contract.py enforces it offline on every pytest run: the backend still serves every route in `rest`, the manifest matches the VENDORED snapshot's surface exactly, and every payload_invariant holds against the typed response models. The non-blocking CI job check_milo_ios_freshness.py re-clones the real app and warns when vendor/milo-ios/ has fallen behind. Refresh the snapshot and this file TOGETHER, consciously, in one commit.",

  "_surface_is_declared_not_reached": "The extractor reads the routes MiloAPIClient declares, which is the right granularity for a contract — the app ships that code, and a backend free to delete a route it targets is the hazard. Two of the six are currently declared but invoked by nothing in the app: `getAudioState` and `getDockApps` have no caller in any of the 17 Swift files. They are pinned anyway, because 'no caller today' is a fact about this build and not about the dependency.",

  "_why_this_exists": "cf430ae6 deleted GET /api/settings/dock-apps as 'Milo-Mac-only' once the Mac had converged on /bulk. Milo-iOS' client layer targets it too, and nothing in this checkout recorded that. The break is latent rather than live — no caller reaches getDockApps in the published build — so nobody is broken today; the point is that nobody could have known either way. A route with no caller in frontend/src/ is not dead, it is unwitnessed.",

  "_snapshot": {
    "upstream_commit": "bf969c4ffecb",
    "captured": "2026-09-18",
    "files": ["MiloAPIClient.swift", "Models.swift"],
    "note": "MiloAPIClient.swift names the routes; Models.swift names the response fields the app decodes. Every call goes through MiloAPIClient — the Intents and the widget's timeline provider build no URL of their own, and every path is a literal — so these two files are the whole surface, confirmed by the app's author on 2026-09-18."
  },

  "_broken_calls": {
    "why": "Routes the app's client layer targets that the backend does not serve. They cannot go in `rest` — test_rest_route_exists would fail on a route that genuinely does not exist — and they cannot be dropped, or the contract would be silent about a dependency that is already unmet. So they are listed, and test_broken_calls_are_still_broken fails the moment either side moves: if the backend starts serving it, or the app stops declaring it, the entry is stale and must go. It is an enumerated fact about the client, never a tolerance in the contract — do not add a second section of this kind to paper over a mismatch.",
    "routes": [
      {
        "method": "GET",
        "path": "/api/settings/dock-apps",
        "consumer": "MiloAPIClient.getDockApps",
        "answers": 405,
        "since": "cf430ae6",
        "reached": false,
        "replacement": "Read dock_apps.enabled_apps from GET /api/settings/bulk, already in `rest`. The path survives for PUT, which is why it answers 405 and not 404."
      }
    ]
  },

  "rest": [
    {"method": "GET", "path": "/api/volume/state", "consumer": "MiloAPIClient.getVolume"},
    {"method": "POST", "path": "/api/volume/adjust", "consumer": "MiloAPIClient.fireAdjustVolume",
     "note": "Body {delta_db, show_bar}. Built by hand with URLRequest rather than the get/post helper — the extractor reads both shapes on purpose."},
    {"method": "GET", "path": "/api/settings/bulk", "consumer": "MiloAPIClient.syncVolumeSettings",
     "note": "Also the only surviving read for dock apps: it carries dock_apps.enabled_apps."},
    {"method": "GET", "path": "/api/audio/state", "consumer": "MiloAPIClient.getAudioState",
     "note": "Declared, not reached. Its Codable model (Models.swift::AudioStateResponse) declares a non-optional `status` plus `source`/`title`/`artist`, none of which this route serves — title and artist live inside `metadata`. So the decode would throw if anything called it, and no payload_invariants are listed: asserting fields the app does not read would be inventing a contract rather than recording one."},
    {"method": "POST", "path": "/api/audio/source/{name}", "consumer": "MiloAPIClient.changeSource",
     "note": "Answers 200 with status:'error' on failure, not an HTTP error — the same deliberate exception Milo-Mac depends on."}
  ],

  "payload_invariants": {
    "_about": "The response fields Milo-iOS decodes by name. A route can survive while the field the app reads is renamed, and a Codable struct missing a non-optional key throws rather than degrading — the whole call fails, it does not lose one value. Resolved through the live OpenAPI schema, never a hand-written copy: `a.b` is a property, `a[k]` a dict value (additionalProperties).",
    "schema": {
      "/api/volume/state": ["data.global_volume_db", "data.global_mute", "data.any_volume_control"],
      "/api/settings/bulk": ["volume_limits.min_db", "volume_limits.max_db", "volume_steps.step_mobile_db", "dock_apps.enabled_apps"]
    }
  }
}
